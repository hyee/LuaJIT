name: C/C++ CI

on:
  push:
    branches: [ "v2.1" ]
  pull_request:
    branches: [ "v2.1" ]

jobs:
  build:

    runs-on: macos-11

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: cd src & make
